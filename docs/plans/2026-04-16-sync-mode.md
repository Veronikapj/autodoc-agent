# Sync 모드 구현 계획

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** GitHub Actions UI에서 버튼 하나로 전체 문서를 초기화·동기화하는 `--sync` 모드 추가

**Architecture:** `SyncOrchestrator`가 Phase 1(Triage: Haiku로 각 문서 업데이트 필요 여부 판정)과 Phase 2(필요한 문서만 풀 에이전트 실행)로 나뉘어 동작한다. 기존 specialist 에이전트를 재사용하며 `workflow_dispatch` 트리거로 PR 없이 수동 실행된다.

**Tech Stack:** Kotlin, Koog 0.8.0 (`@ExperimentalAgentsApi`), kohsuke/github-api, JUnit 5

---

### Task 1: GitHubTool에 커밋 로그 조회 추가

**Files:**
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/tools/GitHubTool.kt`
- Modify: `src/test/kotlin/io/github/veronikapj/autodoc/tools/GitHubToolTest.kt`

**Step 1: 테스트 작성**

```kotlin
// GitHubToolTest.kt에 추가
@Test
fun `CommitInfo가 메시지와 SHA를 올바르게 담는다`() {
    val commit = CommitInfo(sha = "abc123", message = "feat: 로그인 추가")
    assertEquals("abc123", commit.sha)
    assertEquals("feat: 로그인 추가", commit.message)
}
```

**Step 2: 테스트 실패 확인**

```bash
./gradlew test --tests "*.GitHubToolTest" 2>&1 | tail -20
```
Expected: FAIL — `CommitInfo` 없음

**Step 3: CommitInfo 데이터 클래스 + fetchRecentCommits 추가**

`GitHubTool.kt`에 추가:
```kotlin
data class CommitInfo(val sha: String, val message: String)

fun fetchRecentCommits(repoName: String, maxCount: Int = 30): List<CommitInfo> {
    val repo = github.getRepository(repoName)
    return repo.listCommits().toList().take(maxCount).map {
        CommitInfo(sha = it.shA1, message = it.commitShortInfo.message)
    }
}
```

**Step 4: 테스트 통과 확인**

```bash
./gradlew test --tests "*.GitHubToolTest" 2>&1 | tail -10
```
Expected: PASS

**Step 5: 커밋**

```bash
git add src/main/kotlin/io/github/veronikapj/autodoc/tools/GitHubTool.kt \
        src/test/kotlin/io/github/veronikapj/autodoc/tools/GitHubToolTest.kt
git commit -m "feat: GitHubTool에 최근 커밋 로그 조회 기능 추가"
```

---

### Task 2: BaseDocAgent에 탐색 범위 힌트 추가

**Files:**
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/BaseDocAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/ReadmeAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/ArchDocAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/ApiDocAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/TestDocAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/ChangelogAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/SetupDocAgent.kt`
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/SpecDocAgent.kt`

**Step 1: BaseDocAgent에 abstract 프로퍼티 추가**

```kotlin
// BaseDocAgent.kt
/** sync 모드에서 에이전트가 우선 탐색할 파일 패턴 힌트 */
abstract val searchScopeHint: String
```

**Step 2: 각 specialist에 searchScopeHint 구현**

```kotlin
// ReadmeAgent.kt
override val searchScopeHint = "Main.kt, *Application.kt, build.gradle.kts, settings.gradle.kts, README.md"

// ArchDocAgent.kt
override val searchScopeHint = "전체 디렉터리 구조, *Module*.kt, *di/*.kt, build.gradle.kts"

// ApiDocAgent.kt
override val searchScopeHint = "*Api.kt, *Controller.kt, *Service.kt, *Endpoint*.kt, *Repository.kt"

// TestDocAgent.kt
override val searchScopeHint = "*Test.kt, *Spec.kt, *Fake*.kt, *Mock*.kt"

// ChangelogAgent.kt
override val searchScopeHint = "CHANGELOG.md, 커밋 로그만 참고 (코드 탐색 최소화)"

// SetupDocAgent.kt
override val searchScopeHint = "build.gradle.kts, settings.gradle.kts, Dockerfile, docker-compose.yml, .env.example"

// SpecDocAgent.kt
override val searchScopeHint = "docs/spec/, .autodoc/config.yml"
```

**Step 3: 빌드 통과 확인**

```bash
./gradlew build 2>&1 | tail -10
```
Expected: BUILD SUCCESSFUL

**Step 4: 커밋**

```bash
git add src/main/kotlin/io/github/veronikapj/autodoc/agent/specialist/
git commit -m "feat: 에이전트별 sync 탐색 범위 힌트(searchScopeHint) 추가"
```

---

### Task 3: TriageAgent 구현

**Files:**
- Create: `src/main/kotlin/io/github/veronikapj/autodoc/agent/TriageAgent.kt`
- Create: `src/test/kotlin/io/github/veronikapj/autodoc/agent/TriageAgentTest.kt`

**Step 1: 테스트 작성**

```kotlin
// TriageAgentTest.kt
package io.github.veronikapj.autodoc.agent

import io.github.veronikapj.autodoc.platform.AgentType
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Assertions.*

class TriageAgentTest {

    @Test
    fun `NEEDED 응답을 올바르게 파싱한다`() {
        val result = TriageAgent.parseResponse("README: NEEDED\nCHANGELOG: SKIP")
        assertEquals(TriageResult.NEEDED, result[AgentType.README])
        assertEquals(TriageResult.SKIP, result[AgentType.CHANGELOG])
    }

    @Test
    fun `파싱 실패 시 NEEDED로 폴백한다`() {
        val result = TriageAgent.parseResponse("README: GARBAGE")
        assertEquals(TriageResult.NEEDED, result[AgentType.README])
    }

    @Test
    fun `응답에 없는 AgentType은 NEEDED로 폴백한다`() {
        val result = TriageAgent.parseResponse("")
        AgentType.entries.forEach {
            assertEquals(TriageResult.NEEDED, result[it])
        }
    }
}
```

**Step 2: 테스트 실패 확인**

```bash
./gradlew test --tests "*.TriageAgentTest" 2>&1 | tail -20
```
Expected: FAIL

**Step 3: TriageAgent 구현**

```kotlin
// TriageAgent.kt
@file:OptIn(ExperimentalAgentsApi::class)

package io.github.veronikapj.autodoc.agent

import ai.koog.agents.core.annotation.ExperimentalAgentsApi
import ai.koog.prompt.executor.llms.MultiLLMPromptExecutor
import ai.koog.prompt.executor.clients.anthropic.AnthropicModels
import ai.koog.agents.core.agent.AIAgent
import ai.koog.agents.core.agent.config.AIAgentConfig
import ai.koog.agents.core.tools.ToolRegistry
import ai.koog.prompt.dsl.prompt
import ai.koog.prompt.executor.clients.anthropic.AnthropicParams
import io.github.veronikapj.autodoc.platform.AgentType
import io.github.veronikapj.autodoc.tools.CommitInfo
import org.slf4j.LoggerFactory

enum class TriageResult { NEEDED, SKIP }

class TriageAgent(private val executor: MultiLLMPromptExecutor) {

    private val log = LoggerFactory.getLogger(javaClass)

    suspend fun triage(
        commits: List<CommitInfo>,
        existingDocs: Map<AgentType, String?>,
    ): Map<AgentType, TriageResult> {
        val commitSummary = commits.take(30).joinToString("\n") { "- ${it.message}" }
        val docSummary = existingDocs.entries.joinToString("\n") { (type, content) ->
            if (content == null) "${type.name}: 문서 없음"
            else "${type.name}: 존재 (${content.length}자)"
        }

        val request = """
            최근 커밋 로그:
            $commitSummary

            현재 문서 상태:
            $docSummary

            각 문서가 최신 커밋 내용을 반영하고 있는지 판단하여 아래 형식으로만 응답하세요.
            문서가 없으면 반드시 NEEDED입니다.
            응답 예시:
            README: NEEDED
            ARCH_DOC: SKIP
            API_DOC: NEEDED
            TEST_DOC: SKIP
            CHANGELOG: NEEDED
            SETUP_DOC: SKIP
            SPEC_DOC: SKIP
        """.trimIndent()

        val agent = AIAgent(
            promptExecutor = executor,
            agentConfig = AIAgentConfig(
                prompt = prompt("triage", params = AnthropicParams(maxTokens = 512)) {
                    system("""
                        당신은 문서 업데이트 필요 여부를 판단하는 에이전트입니다.
                        반드시 각 항목을 "AGENT_TYPE: NEEDED" 또는 "AGENT_TYPE: SKIP" 형식으로만 응답합니다.
                        설명, 이유, 부가 텍스트 없이 오직 판정 결과만 출력합니다.
                    """.trimIndent())
                },
                model = AnthropicModels.Haiku_4_5,
                maxAgentIterations = 1,
            ),
            toolRegistry = ToolRegistry { },
        )

        val response = agent.run(request)
        log.info("triage response: {}", response)
        return parseResponse(response)
    }

    companion object {
        fun parseResponse(response: String): Map<AgentType, TriageResult> {
            val parsed = response.lines()
                .mapNotNull { line ->
                    val parts = line.trim().split(":")
                    if (parts.size != 2) return@mapNotNull null
                    val type = runCatching {
                        AgentType.valueOf(parts[0].trim())
                    }.getOrNull() ?: return@mapNotNull null
                    val result = when (parts[1].trim()) {
                        "SKIP" -> TriageResult.SKIP
                        else -> TriageResult.NEEDED
                    }
                    type to result
                }.toMap()

            return AgentType.entries.associateWith { parsed[it] ?: TriageResult.NEEDED }
        }
    }
}
```

**Step 4: 테스트 통과 확인**

```bash
./gradlew test --tests "*.TriageAgentTest" 2>&1 | tail -10
```
Expected: PASS

**Step 5: 커밋**

```bash
git add src/main/kotlin/io/github/veronikapj/autodoc/agent/TriageAgent.kt \
        src/test/kotlin/io/github/veronikapj/autodoc/agent/TriageAgentTest.kt
git commit -m "feat: TriageAgent 구현 (Haiku 기반 NEEDED/SKIP 판정)"
```

---

### Task 4: SyncOrchestrator 구현

**Files:**
- Create: `src/main/kotlin/io/github/veronikapj/autodoc/agent/SyncOrchestrator.kt`
- Create: `src/test/kotlin/io/github/veronikapj/autodoc/agent/SyncOrchestratorTest.kt`

**Step 1: 테스트 작성**

```kotlin
// SyncOrchestratorTest.kt
package io.github.veronikapj.autodoc.agent

import io.github.veronikapj.autodoc.platform.AgentType
import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Assertions.*

class SyncOrchestratorTest {

    @Test
    fun `NEEDED인 에이전트만 실행 대상에 포함된다`() {
        val triageResults = mapOf(
            AgentType.README to TriageResult.NEEDED,
            AgentType.CHANGELOG to TriageResult.SKIP,
            AgentType.ARCH_DOC to TriageResult.NEEDED,
        )
        val toRun = SyncOrchestrator.filterNeeded(triageResults)
        assertTrue(AgentType.README in toRun)
        assertTrue(AgentType.ARCH_DOC in toRun)
        assertFalse(AgentType.CHANGELOG in toRun)
    }

    @Test
    fun `sync 요청 메시지에 커밋 로그와 탐색 범위 힌트가 포함된다`() {
        val request = SyncOrchestrator.buildSyncRequest(
            agentType = AgentType.API_DOC,
            commitLog = "- feat: API 추가",
            searchScopeHint = "*Api.kt, *Controller.kt",
            existingContent = null,
        )
        assertTrue("feat: API 추가" in request)
        assertTrue("*Api.kt" in request)
        assertTrue("문서 없음" in request)
    }
}
```

**Step 2: 테스트 실패 확인**

```bash
./gradlew test --tests "*.SyncOrchestratorTest" 2>&1 | tail -20
```
Expected: FAIL

**Step 3: SyncOrchestrator 구현**

```kotlin
// SyncOrchestrator.kt
package io.github.veronikapj.autodoc.agent

import io.github.veronikapj.autodoc.a2a.A2AClientManager
import io.github.veronikapj.autodoc.agent.specialist.BaseDocAgent
import io.github.veronikapj.autodoc.platform.AgentType
import io.github.veronikapj.autodoc.tools.CommitInfo
import io.github.veronikapj.autodoc.tools.GitHubTool
import kotlinx.coroutines.async
import kotlinx.coroutines.awaitAll
import kotlinx.coroutines.coroutineScope
import org.slf4j.LoggerFactory

class SyncOrchestrator(
    private val triageAgent: TriageAgent,
    private val clientManager: A2AClientManager,
    private val specialists: Map<AgentType, BaseDocAgent>,
    private val githubTool: GitHubTool,
    private val repoName: String,
) {
    private val log = LoggerFactory.getLogger(javaClass)

    suspend fun run(): Map<String, String> = coroutineScope {
        // Phase 1: 커밋 로그 + 기존 문서 수집
        val commits = githubTool.fetchRecentCommits(repoName)
        log.info("fetched {} recent commits", commits.size)

        val existingDocs = AgentType.entries.associateWith { agentType ->
            runCatching {
                githubTool.fetchFileContent(repoName, agentType.toDocPath())
            }.getOrNull()
        }

        // Phase 1: Triage
        log.info("phase 1: triage...")
        val triageResults = triageAgent.triage(commits, existingDocs)
        val needed = filterNeeded(triageResults)

        if (needed.isEmpty()) {
            log.info("all documents are up to date, skipping")
            return@coroutineScope emptyMap()
        }

        log.info("phase 2: running {} agent(s): {}", needed.size, needed.joinToString { it.name })
        val commitLog = commits.take(30).joinToString("\n") { "- ${it.message}" }

        // Phase 2: 필요한 에이전트만 병렬 실행
        val results = needed.map { agentType ->
            async {
                val specialist = specialists[agentType] ?: return@async agentType to null
                val request = buildSyncRequest(
                    agentType = agentType,
                    commitLog = commitLog,
                    searchScopeHint = specialist.searchScopeHint,
                    existingContent = existingDocs[agentType],
                )
                runCatching {
                    val result = clientManager.sendMessage(agentType, request)
                    log.info("{} agent completed", agentType.name)
                    agentType to result
                }.getOrElse { e ->
                    log.error("{} agent failed: {}", agentType.name, e.message)
                    agentType to null
                }
            }
        }.awaitAll()

        results
            .filter { (_, content) -> content != null }
            .associate { (agentType, content) -> agentType.toDocPath() to content!! }
    }

    companion object {
        fun filterNeeded(triageResults: Map<AgentType, TriageResult>): List<AgentType> =
            triageResults.filter { (_, result) -> result == TriageResult.NEEDED }.keys.toList()

        fun buildSyncRequest(
            agentType: AgentType,
            commitLog: String,
            searchScopeHint: String,
            existingContent: String?,
        ) = """
            [SYNC 모드] ${agentType.name} 문서 동기화 요청

            최근 커밋 로그:
            $commitLog

            우선 탐색 범위: $searchScopeHint

            기존 문서 상태: ${if (existingContent == null) "문서 없음 — 새로 생성하세요" else "존재함 — 업데이트가 필요한 부분만 수정하세요"}

            코드를 탐색하여 문서를 최신 상태로 만들어주세요.
            출력은 반드시 마크다운 문서 내용만 포함하세요. 설명, 코멘트 절대 금지.
        """.trimIndent()
    }
}
```

**Step 4: GitHubTool에 fetchFileContent 추가**

```kotlin
// GitHubTool.kt에 추가
fun fetchFileContent(repoName: String, path: String): String? =
    runCatching {
        github.getRepository(repoName).getFileContent(path).content
            ?.let { String(java.util.Base64.getDecoder().decode(it.replace("\n", ""))) }
    }.getOrNull()
```

**Step 5: 테스트 통과 확인**

```bash
./gradlew test --tests "*.SyncOrchestratorTest" 2>&1 | tail -10
```
Expected: PASS

**Step 6: 커밋**

```bash
git add src/main/kotlin/io/github/veronikapj/autodoc/agent/SyncOrchestrator.kt \
        src/test/kotlin/io/github/veronikapj/autodoc/agent/SyncOrchestratorTest.kt \
        src/main/kotlin/io/github/veronikapj/autodoc/tools/GitHubTool.kt
git commit -m "feat: SyncOrchestrator 구현 (Phase 1 Triage + Phase 2 병렬 실행)"
```

---

### Task 5: Main.kt에 --sync 분기 추가

**Files:**
- Modify: `src/main/kotlin/io/github/veronikapj/autodoc/Main.kt`

**Step 1: --sync 분기 추가**

기존 `fun main(args: Array<String>)` 시작 부분을 수정:

```kotlin
fun main(args: Array<String>) = runBlocking {
    val isSyncMode = args.contains("--sync")
    val prNumber = if (isSyncMode) null
    else args.firstOrNull()?.toIntOrNull()
        ?: error("PR 번호를 인자로 전달해주세요. 예: ./gradlew run --args='42'")

    // ... 공통 초기화 코드 (executor, templateResolver, platformConfig, githubTool, specialists) ...

    if (isSyncMode) {
        log.info("starting sync mode — repo={}", repoName)
        val triageAgent = TriageAgent(executor)
        val syncOrchestrator = SyncOrchestrator(triageAgent, clientManager, specialists, githubTool, repoName)
        val changedDocs = syncOrchestrator.run()

        if (changedDocs.isEmpty()) {
            log.info("all documents are up to date, exiting")
            return@runBlocking
        }

        log.info("{} document(s) to update: {}", changedDocs.size, changedDocs.keys.joinToString())
        val summary = buildSyncPRSummary(changedDocs.keys.toList())
        githubTool.createDocsPR(repoName, "main", 0, changedDocs, summary)
        log.info("done")
    } else {
        // 기존 PR 모드 로직
        val orchestrator = OrchestratorAgent(clientManager, platformConfig, githubTool, repoName)
        val changedDocs = orchestrator.run(prNumber!!)
        // ...
    }
}

private fun buildSyncPRSummary(updatedDocs: List<String>) = """
    ## AutoDoc Agent 문서 동기화

    전체 코드베이스를 분석하여 다음 문서를 업데이트했습니다:

    ${updatedDocs.joinToString("\n") { "- `$it`" }}

    > 내용을 검토 후 머지해주세요.

    Generated by [AutoDoc Agent](https://github.com/Veronikapj/autodoc-agent)
""".trimIndent()
```

**Step 2: GitHubTool.createDocsPR의 브랜치명 처리**

`createDocsPR`에서 `prNumber = 0`일 때 브랜치명 분기:

```kotlin
val branchName = if (prNumber == 0) "docs/auto-sync"
else "docs/auto-update-pr-$prNumber"
```

**Step 3: 빌드 확인**

```bash
./gradlew build 2>&1 | tail -10
```
Expected: BUILD SUCCESSFUL

**Step 4: 커밋**

```bash
git add src/main/kotlin/io/github/veronikapj/autodoc/Main.kt \
        src/main/kotlin/io/github/veronikapj/autodoc/tools/GitHubTool.kt
git commit -m "feat: Main.kt에 --sync 모드 분기 추가"
```

---

### Task 6: autodoc-sync.yml 워크플로우 추가

**Files:**
- Create: `.github/workflows/autodoc-sync.yml`

**Step 1: 워크플로우 파일 생성**

```yaml
# .github/workflows/autodoc-sync.yml
name: AutoDoc Sync

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: '전체 문서 동기화를 실행합니다. (yes 입력)'
        required: true
        default: 'yes'

jobs:
  autodoc-sync:
    if: github.event.inputs.confirm == 'yes'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Run AutoDoc Sync
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          TARGET_REPO_PATH: ${{ github.workspace }}
        run: ./gradlew run --args="--sync"
```

**Step 2: 전체 테스트 실행**

```bash
./gradlew test 2>&1 | tail -20
```
Expected: BUILD SUCCESSFUL, 모든 테스트 PASS

**Step 3: 커밋**

```bash
git add .github/workflows/autodoc-sync.yml
git commit -m "feat: autodoc-sync workflow 추가 (workflow_dispatch 수동 실행)"
```

---

### Task 7: 전체 검증 및 PR 업데이트

**Step 1: 전체 빌드 + 테스트**

```bash
./gradlew test shadowJar 2>&1 | tail -20
```
Expected: BUILD SUCCESSFUL

**Step 2: push**

```bash
git push origin refactor/kotlin-conventions
```

**Step 3: PR #6 확인**

변경된 파일 목록이 설계와 일치하는지 확인:
- `GitHubTool.kt` (커밋 조회, 파일 내용 조회, 브랜치명 분기)
- `BaseDocAgent.kt` + 각 specialist (searchScopeHint)
- `TriageAgent.kt` (신규)
- `SyncOrchestrator.kt` (신규)
- `Main.kt` (--sync 분기)
- `autodoc-sync.yml` (신규)
